(() => {
  "use strict";
  const $=(s,r=document)=>r.querySelector(s),$$=(s,r=document)=>[...r.querySelectorAll(s)];
  let snapshot=null,activeDealId="",pollTimer=null,inboxQuery="";
  const esc=v=>String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[c]));
  function state(){try{return snapshot||(typeof appState!=="undefined"?appState:null)}catch{return snapshot}}
  function toast(text,tone="success"){if(typeof window.showToast==="function")return window.showToast(text,tone);console.log(text)}
  async function request(url,options={}){const opts={credentials:"same-origin",cache:"no-store",...options};if(opts.body&&!(opts.body instanceof FormData))opts.headers={"Content-Type":"application/json",...(opts.headers||{})};let r;try{r=await fetch(url,opts)}catch{throw new Error("No se pudo conectar con el CRM. Verificá la conexión e intentá nuevamente.")}const raw=await r.text();let data={};try{data=raw?JSON.parse(raw):{}}catch{}if(r.status===401){location.assign("/login");throw new Error("La sesión venció. Volvé a ingresar.")}if(!r.ok)throw new Error(data.error||`Error ${r.status}`);return data}
  function sync(next){if(!next)return;snapshot=next;try{if(typeof window.setState==="function")window.setState(next)}catch{}renderInbox();if(activeDealId)renderConversation()}
  async function refresh(){try{sync(await request("/api/state"))}catch(e){if(!String(e.message).includes("sesión"))setError(e.message)}}
  function fmt(value){if(!value)return "";try{return new Intl.DateTimeFormat("es-PY",{day:"2-digit",month:"2-digit",hour:"2-digit",minute:"2-digit"}).format(new Date(value))}catch{return ""}}
  function deals(){return (state()?.deals||[]).slice().sort((a,b)=>String(b.updatedAt||b.lastMessageAt||"").localeCompare(String(a.updatedAt||a.lastMessageAt||"")))}
  function selectedDeal(){return deals().find(d=>d.id===activeDealId)||null}
  function lineName(deal){const s=state();return (s?.whatsappLines||[]).find(l=>l.id===deal?.lineId)?.name||(s?.whatsappLines||[]).find(l=>l.branchId===deal?.branchId&&l.isDefault)?.name||"WhatsApp"}
  function ownerName(deal){return deal?.ownerName||(state()?.users||[]).find(u=>u.id===deal?.ownerUserId)?.name||"Sin responsable"}
  function messageAttachment(a){if(!a)return "";const url=a.url||a.path||a.publicUrl||"";const name=a.fileName||a.name||a.kind||"Adjunto";if(!url)return `<span class="v25-attachment">📎 ${esc(name)}</span>`;if((a.kind||"")==="image"||String(a.mimeType||"").startsWith("image/"))return `<a class="v25-media" href="${esc(url)}" target="_blank" rel="noopener"><img src="${esc(url)}" alt="${esc(name)}"></a>`;return `<a class="v25-attachment" href="${esc(url)}" target="_blank" rel="noopener">📎 ${esc(name)}</a>`}
  function installOverlay(){if($("#v25-chat"))return;const el=document.createElement("section");el.id="v25-chat";el.className="v25-chat";el.hidden=true;el.innerHTML=`<button class="v25-chat-backdrop" data-v25-close aria-label="Cerrar"></button><article class="v25-chat-panel"><header class="v25-chat-head"><div><small id="v25-chat-line">WhatsApp</small><h2 id="v25-chat-name">Conversación</h2><p id="v25-chat-meta"></p></div><div class="v25-chat-actions"><button type="button" id="v25-chat-refresh">↻ Actualizar</button><button type="button" data-v25-close>✕ Cerrar</button></div></header><div class="v25-chat-status" id="v25-chat-status"></div><main class="v25-message-list" id="v25-messages"></main><form class="v25-composer" id="v25-composer"><textarea id="v25-message" rows="2" placeholder="Escribí tu respuesta al cliente…" maxlength="4000"></textarea><button type="submit" id="v25-send">Enviar</button><div class="v25-chat-error" id="v25-chat-error"></div></form></article>`;document.body.appendChild(el);el.addEventListener("click",e=>{if(e.target.closest("[data-v25-close]"))closeConversation()});$("#v25-chat-refresh").onclick=()=>refresh();$("#v25-composer").onsubmit=sendMessage;$("#v25-message").addEventListener("keydown",e=>{if(e.key==="Enter"&&!e.shiftKey&&!e.isComposing){e.preventDefault();$("#v25-composer").requestSubmit()}})}
  function setError(message=""){const el=$("#v25-chat-error");if(el)el.textContent=message}
  function openConversation(id){installOverlay();activeDealId=id;snapshot=null;const chat=$("#v25-chat");chat.hidden=false;document.body.classList.add("v25-chat-open");renderConversation();startPolling();requestAnimationFrame(()=>$("#v25-message")?.focus())}
  function closeConversation(){activeDealId="";const chat=$("#v25-chat");if(chat)chat.hidden=true;document.body.classList.remove("v25-chat-open");stopPolling()}
  function renderConversation(){const deal=selectedDeal();if(!deal){if(activeDealId)setError("La negociación ya no está disponible para este usuario.");return}$("#v25-chat-line").textContent=lineName(deal);$("#v25-chat-name").textContent=deal.name||deal.phone||"Cliente";$("#v25-chat-meta").textContent=`${deal.phone||"Sin teléfono"} · ${ownerName(deal)}`;const stage=deal.stage||"new";$("#v25-chat-status").innerHTML=`<span>Estado: <b>${esc(stage)}</b></span><span>Bot: <b>${deal.botActive?"Activo":"Copiloto / pausado"}</b></span><span>Responsable: <b>${esc(ownerName(deal))}</b></span>`;const list=$("#v25-messages"),messages=(deal.messages||[]).slice(-150);list.innerHTML=messages.length?messages.map(m=>`<article class="v25-message ${m.direction==="outgoing"?"out":m.direction==="system"?"system":"in"}">${messageAttachment(m.attachment)}${m.text?`<p>${esc(m.text)}</p>`:""}<small>${m.direction==="outgoing"?esc(m.agentName||m.userName||m.origin||"Asesor"):m.direction==="system"?"Sistema":"Cliente"} · ${esc(fmt(m.at||m.createdAt))}</small></article>`).join(""):`<div class="v25-empty">Todavía no hay mensajes guardados en esta conversación.</div>`;requestAnimationFrame(()=>{list.scrollTop=list.scrollHeight});setError("")}
  async function sendMessage(event){event.preventDefault();const deal=selectedDeal(),box=$("#v25-message"),button=$("#v25-send"),text=box.value.trim();if(!deal)return setError("Volvé a abrir la conversación.");if(!text)return setError("Escribí un mensaje.");button.disabled=true;button.textContent="Enviando…";setError("");try{const next=await request(`/api/deals/${encodeURIComponent(deal.id)}/message`,{method:"POST",body:JSON.stringify({text})});box.value="";sync(next);toast("Mensaje enviado") }catch(e){setError(e.message);toast(e.message,"warning")}finally{button.disabled=false;button.textContent="Enviar"}}
  function installInbox(){const view=$("[data-view-panel='whatsapp']");if(!view||$("#v25-whatsapp-inbox"))return;const box=document.createElement("section");box.id="v25-whatsapp-inbox";box.className="v25-inbox-panel";box.innerHTML=`<header><div><p>CONVERSACIONES · V25</p><h2>Responder clientes desde WhatsApp y bot</h2><small>Misma conversación que Negociaciones. No se crea una copia ni una bandeja paralela.</small></div><button type="button" id="v25-inbox-refresh">↻ Actualizar</button></header><div class="v25-inbox-toolbar"><input id="v25-inbox-search" type="search" placeholder="Buscar cliente, teléfono o mensaje…"><span id="v25-inbox-count">0 conversaciones</span></div><div class="v25-inbox-list" id="v25-inbox-list"></div>`;view.prepend(box);$("#v25-inbox-refresh").onclick=()=>refresh();$("#v25-inbox-search").addEventListener("input",e=>{inboxQuery=e.target.value.trim().toLowerCase();renderInbox()});$("#v25-inbox-list").addEventListener("click",e=>{const row=e.target.closest("[data-v25-deal]");if(row)openConversation(row.dataset.v25Deal)});renderInbox()}
  function renderInbox(){installInbox();const list=$("#v25-inbox-list");if(!list)return;const q=inboxQuery;const rows=deals().filter(d=>!q||[d.name,d.phone,d.lastMessage,ownerName(d)].some(v=>String(v||"").toLowerCase().includes(q)));$("#v25-inbox-count").textContent=`${rows.length} conversaciones`;list.innerHTML=rows.length?rows.slice(0,250).map(d=>{const last=(d.messages||[]).at(-1);return `<button type="button" class="v25-inbox-row" data-v25-deal="${esc(d.id)}"><span class="v25-avatar">${esc(String(d.name||d.phone||"C").trim().charAt(0).toUpperCase())}</span><span class="v25-inbox-copy"><b>${esc(d.name||d.phone||"Cliente")}</b><small>${esc(d.phone||"")} · ${esc(ownerName(d))}</small><em>${esc(last?.text||d.lastMessage||"Sin mensajes")}</em></span><span class="v25-inbox-side"><small>${esc(fmt(last?.at||d.updatedAt))}</small><b>${esc(d.stage||"new")}</b></span></button>`}).join(""):`<div class="v25-empty">No hay conversaciones visibles para este usuario.</div>`}
  function installNegotiationOpen(){document.addEventListener("click",e=>{const card=e.target.closest?.("#crm-board [data-deal-id]");if(!card||e.target.closest("button,a,input,select,textarea"))return;e.preventDefault();e.stopPropagation();e.stopImmediatePropagation();openConversation(card.dataset.dealId)},true)}
  function startPolling(){stopPolling();pollTimer=setInterval(()=>{if(activeDealId)void refresh()},3500)}function stopPolling(){if(pollTimer){clearInterval(pollTimer);pollTimer=null}}
  function install(){installOverlay();installInbox();installNegotiationOpen();window.addEventListener("crm:state",()=>{snapshot=null;renderInbox();if(activeDealId)renderConversation()});document.addEventListener("visibilitychange",()=>{if(!document.hidden&&activeDealId)void refresh()});document.body.classList.add("v25-ready");setTimeout(()=>renderInbox(),700)}
  if(document.readyState==="loading")document.addEventListener("DOMContentLoaded",install,{once:true});else install();
})();
